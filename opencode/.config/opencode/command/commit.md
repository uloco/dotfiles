---
description: Tighten new comments, then split changes into small, focused commits
---

Use the `tighten-comments` skill first (scoped to the pending uncommitted
diff), then use the `clean-commits` skill to split and commit the changes.
